<form method="post" action="{{ path('app_deck_delete', {'id': deck.id}) }}" onsubmit="return confirm('T\'es sur que tu veux supprimer ce deck :\'(?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deck.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
