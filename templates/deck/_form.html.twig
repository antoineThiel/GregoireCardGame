{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
    <a class="btn btn-primary" href="{{ path('app_deck_index') }}">Retour a la liste</a>

    {% if app.request.attributes.get('_route') == 'app_deck_edit' %}
        {{ include('deck/_delete_form.html.twig') }}
    {% endif %}
{{ form_end(form) }}
